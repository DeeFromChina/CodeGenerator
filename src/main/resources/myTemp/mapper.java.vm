package ${mapperClass.classPath};

import ${mapperClass.entityClass.classPath}.${mapperClass.entityClass.className};
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import org.apache.ibatis.annotations.Mapper;
import org.springframework.stereotype.Repository;

/**
 * @auther ${author}
 * @create ${createTime}
 * @describe
 */
 @Mapper
 @Repository
public interface ${mapperClass.className} extends BaseMapper<${mapperClass.entityClass.className}>{

}