package ${service.servicePath};

import ${service.entityClass.classPath}.${service.entityClass.className};
import com.baomidou.mybatisplus.extension.service.IService;

/**
 * @auther ${author}
 * @create ${createTime}
 * @describe
 */
public interface ${service.serviceName} extends IService<${service.entityClass.className}> {

}
